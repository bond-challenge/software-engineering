Interface -> This is the interface class which contains the definition of all methods
implementation: This is the class which is used to implement the interface
Methods Description:

display()-> This method display the whole data structure
lastnaverage()-> This method takes a number of last element 'N' as a parameter and return moving average of last N elements
add() ->This method takes the element as an argument to be added in the data structure
getaccess() -> provide index position as the argument and it will return the value for that position